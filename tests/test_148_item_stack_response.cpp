#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ItemStackResponsePacket: id + round-trip with empty responses")
{
    STATIC_REQUIRE(bp::ItemStackResponsePacket::Id == 148);

    bp::ItemStackResponsePacket pkt;
    pkt.responses = {};

    // generated by gophertunnel:
    // packet.ItemStackResponse{Responses: []}
    const std::vector<std::uint8_t> golden{
        0x00,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ItemStackResponsePacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->responses.empty());
}
