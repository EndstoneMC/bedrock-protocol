#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("SettingsCommandPacket: id + round-trip with the daytime cycle command")
{
    STATIC_REQUIRE(bp::SettingsCommandPacket::Id == 140);

    bp::SettingsCommandPacket pkt;
    pkt.command_string = "/time set day";
    pkt.suppress_output = true;

    // generated by gophertunnel:
    // packet.SettingsCommand{CommandLine: "/time set day", SuppressOutput: true}
    const std::vector<std::uint8_t> golden{
        0x0D, 0x2F, 0x74, 0x69, 0x6D, 0x65, 0x20, 0x73, 0x65, 0x74, 0x20, 0x64,
        0x61, 0x79, 0x01,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::SettingsCommandPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->command_string == "/time set day");
    REQUIRE(rt->suppress_output);
}
