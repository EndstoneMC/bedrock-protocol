#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ChangeMobPropertyPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::ChangeMobPropertyPacket::Id == 182);

    bp::ChangeMobPropertyPacket pkt;
    pkt.actor_id = static_cast<bp::ActorUniqueID>(-3);
    pkt.prop_name = "size";
    pkt.bool_component_val = true;
    pkt.string_component_val = "large";
    pkt.int_component_val = 5;
    pkt.float_component_val = 1.5f;

    // generated by gophertunnel:
    // packet.ChangeMobProperty{EntityUniqueID: -3, Property: "size",
    //   BoolValue: true, StringValue: "large", IntValue: 5, FloatValue: 1.5}
    const std::vector<std::uint8_t> golden{
        0x05, 0x04, 0x73, 0x69, 0x7A, 0x65, 0x01, 0x05, 0x6C, 0x61, 0x72, 0x67,
        0x65, 0x0A, 0x00, 0x00, 0xC0, 0x3F,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ChangeMobPropertyPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->prop_name == "size");
    REQUIRE(rt->bool_component_val == true);
    REQUIRE(rt->int_component_val == 5);
    REQUIRE(rt->float_component_val == 1.5f);
}
