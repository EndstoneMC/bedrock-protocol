#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("NetworkStackLatencyPacket: round-trip")
{
    STATIC_REQUIRE(bp::NetworkStackLatencyPacket::Id == 115);

    bp::NetworkStackLatencyPacket pkt;
    pkt.create_time = 0x123456789ABCLL;
    pkt.from_server = true;

    // generated by gophertunnel:
    // packet.NetworkStackLatency{Timestamp: 0x123456789ABC, NeedsResponse: true}
    const std::vector<std::uint8_t> golden{
        0xBC, 0x9A, 0x78, 0x56, 0x34, 0x12, 0x00, 0x00, 0x01,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::NetworkStackLatencyPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->create_time == 0x123456789ABCLL);
    REQUIRE(rt->from_server == true);
}
