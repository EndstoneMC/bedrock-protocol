#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ResourcePacksReadyForValidationPacket: empty round-trip")
{
    STATIC_REQUIRE(bp::ResourcePacksReadyForValidationPacket::Id == 340);

    bp::ResourcePacksReadyForValidationPacket pkt;

    // generated by gophertunnel:
    // packet.ResourcePacksReadyForValidation{}
    const std::vector<std::uint8_t> golden{};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ResourcePacksReadyForValidationPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
}
