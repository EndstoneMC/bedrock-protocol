#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ClientboundControlSchemeSetPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::ClientboundControlSchemeSetPacket::Id == 327);

    bp::ClientboundControlSchemeSetPacket pkt;
    pkt.control_scheme = bp::ClientboundControlSchemeSetPacket::Scheme::PlayerRelative;

    // generated by gophertunnel:
    // packet.ClientBoundControlSchemeSet{ControlScheme: ControlSchemePlayerRelative}
    const std::vector<std::uint8_t> golden{0x03};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ClientboundControlSchemeSetPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->control_scheme == bp::ClientboundControlSchemeSetPacket::Scheme::PlayerRelative);
}
