#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("MovementEffectPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::MovementEffectPacket::Id == 318);

    bp::MovementEffectPacket pkt;
    pkt.runtime_id = static_cast<bp::ActorRuntimeID>(7);
    pkt.effect_type = bp::MovementEffectType::GlideBoost;
    pkt.effect_duration = 20;
    pkt.tick = 12;

    // generated by gophertunnel:
    // packet.MovementEffect{EntityRuntimeID: 7, Type: MovementEffectTypeGlideBoost,
    //   Duration: 20, Tick: 12}
    const std::vector<std::uint8_t> golden{0x07, 0x00, 0x28, 0x0C};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::MovementEffectPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->effect_type == bp::MovementEffectType::GlideBoost);
    REQUIRE(rt->effect_duration == 20);
    REQUIRE(rt->tick == 12);
}
