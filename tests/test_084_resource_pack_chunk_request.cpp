#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ResourcePackChunkRequestPacket: round-trip")
{
    STATIC_REQUIRE(bp::ResourcePackChunkRequestPacket::Id == 84);

    bp::ResourcePackChunkRequestPacket pkt;
    pkt.resource_name = "abc";
    pkt.chunk = 42;

    // generated by gophertunnel:
    // packet.ResourcePackChunkRequest{UUID: "abc", ChunkIndex: 42}
    const std::vector<std::uint8_t> golden{
        0x03, 0x61, 0x62, 0x63, 0x2A, 0x00, 0x00, 0x00,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ResourcePackChunkRequestPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->resource_name == "abc");
    REQUIRE(rt->chunk == 42);
}
