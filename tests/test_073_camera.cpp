#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("CameraPacket: id + round-trip with two ActorUniqueIDs")
{
    STATIC_REQUIRE(bp::CameraPacket::Id == 73);

    bp::CameraPacket pkt;
    pkt.camera_id = static_cast<bp::ActorUniqueID>(11);
    pkt.target_player_id = static_cast<bp::ActorUniqueID>(22);

    // generated by gophertunnel:
    // packet.Camera{CameraEntityUniqueID: 11, TargetPlayerUniqueID: 22}
    const std::vector<std::uint8_t> golden{0x16, 0x2C};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::CameraPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->camera_id == 11);
    REQUIRE(rt->target_player_id == 22);
}
