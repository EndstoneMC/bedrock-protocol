#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("GuiDataPickItemPacket: id + round-trip (little-endian int32 slot)")
{
    STATIC_REQUIRE(bp::GuiDataPickItemPacket::Id == 54);

    bp::GuiDataPickItemPacket pkt;
    pkt.item_name = "torch";
    pkt.item_effect_name = "holdable";
    pkt.slot = 3;

    // generated by gophertunnel:
    // packet.GUIDataPickItem{ItemName: "torch", ItemEffects: "holdable", HotBarSlot: 3}
    const std::vector<std::uint8_t> golden{
        0x05, 0x74, 0x6F, 0x72, 0x63, 0x68, 0x08, 0x68, 0x6F, 0x6C, 0x64, 0x61,
        0x62, 0x6C, 0x65, 0x03, 0x00, 0x00, 0x00,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::GuiDataPickItemPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->item_name == "torch");
    REQUIRE(rt->slot == 3);
}
