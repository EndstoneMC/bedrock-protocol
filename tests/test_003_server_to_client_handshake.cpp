#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ServerToClientHandshakePacket: round-trip")
{
    STATIC_REQUIRE(bp::ServerToClientHandshakePacket::Id == 3);

    bp::ServerToClientHandshakePacket pkt;
    pkt.data = "hello";

    // generated by gophertunnel:
    // packet.ServerToClientHandshake{JWT: []byte("hello")}
    const std::vector<std::uint8_t> golden{
        0x05, 0x68, 0x65, 0x6C, 0x6C, 0x6F,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ServerToClientHandshakePacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->data == "hello");
}
