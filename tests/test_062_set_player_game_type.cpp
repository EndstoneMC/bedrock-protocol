#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("SetPlayerGameTypePacket: id + round-trip of a single varint32 enum")
{
    STATIC_REQUIRE(bp::SetPlayerGameTypePacket::Id == 62);

    bp::SetPlayerGameTypePacket pkt;
    pkt.player_game_type = bp::GameType::Creative;

    // generated by gophertunnel:
    // packet.SetPlayerGameType{GameType: 1}
    const std::vector<std::uint8_t> golden{
        0x02,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::SetPlayerGameTypePacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->player_game_type == bp::GameType::Creative);
}
