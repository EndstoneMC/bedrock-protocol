#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ChunkRadiusUpdatedPacket: id + round-trip of a single varint32 radius")
{
    STATIC_REQUIRE(bp::ChunkRadiusUpdatedPacket::Id == 70);

    bp::ChunkRadiusUpdatedPacket pkt;
    pkt.chunk_radius = 12;

    // generated by gophertunnel:
    // packet.ChunkRadiusUpdated{ChunkRadius: 12}
    const std::vector<std::uint8_t> golden{
        0x18,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ChunkRadiusUpdatedPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->chunk_radius == 12);
}
