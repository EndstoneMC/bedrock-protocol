#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("OnScreenTextureAnimationPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::OnScreenTextureAnimationPacket::Id == 130);

    bp::OnScreenTextureAnimationPacket pkt;
    pkt.effect_id = 42;

    // generated by gophertunnel:
    // packet.OnScreenTextureAnimation{AnimationType: 42}
    const std::vector<std::uint8_t> golden{
        0x2A, 0x00, 0x00, 0x00,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::OnScreenTextureAnimationPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->effect_id == 42);
}
