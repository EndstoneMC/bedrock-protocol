#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("AddBehaviorTreePacket: id + round-trip")
{
    STATIC_REQUIRE(bp::AddBehaviorTreePacket::Id == 89);

    bp::AddBehaviorTreePacket pkt;
    pkt.json_input = R"({"id":"test"})";

    // generated by gophertunnel:
    // packet.AddBehaviourTree{BehaviourTree: `{"id":"test"}`}
    const std::vector<std::uint8_t> golden{
        0x0D, 0x7B, 0x22, 0x69, 0x64, 0x22, 0x3A, 0x22, 0x74, 0x65, 0x73, 0x74,
        0x22, 0x7D,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::AddBehaviorTreePacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->json_input == R"({"id":"test"})");
}
