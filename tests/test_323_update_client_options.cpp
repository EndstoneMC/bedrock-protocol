#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("UpdateClientOptionsPacket: v975 appends optional filter_profanity bool")
{
    STATIC_REQUIRE(bp::UpdateClientOptionsPacket::Id == 323);

    bp::UpdateClientOptionsPacket pkt;
    pkt.graphics_mode = bp::GraphicsMode::Advanced;
    pkt.filter_profanity = true;

    // generated by gophertunnel:
    // packet.UpdateClientOptions{GraphicsMode: protocol.Option(byte(GraphicsModeAdvanced)),
    //                            FilterProfanity: protocol.Option(true)}
    const std::vector<std::uint8_t> golden{0x01, 0x02, 0x01, 0x01};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::UpdateClientOptionsPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->graphics_mode.has_value());
    REQUIRE(*rt->graphics_mode == bp::GraphicsMode::Advanced);
    REQUIRE(rt->filter_profanity.has_value());
    REQUIRE(*rt->filter_profanity == true);
}
