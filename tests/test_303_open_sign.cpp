#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("OpenSignPacket: id + round-trip with the front-side flag")
{
    STATIC_REQUIRE(bp::OpenSignPacket::Id == 303);

    bp::OpenSignPacket pkt;
    pkt.pos = bp::BlockPos{1, 2, 3};
    pkt.is_front_side = true;

    // generated by gophertunnel:
    // packet.OpenSign{Position: protocol.BlockPos{1, 2, 3}, FrontSide: true}
    const std::vector<std::uint8_t> golden{0x02, 0x04, 0x06, 0x01};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::OpenSignPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->pos.x == 1);
    REQUIRE(rt->is_front_side);
}
