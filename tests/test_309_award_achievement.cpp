#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("AwardAchievementPacket: id + round-trip of a single LE int32")
{
    STATIC_REQUIRE(bp::AwardAchievementPacket::Id == 309);

    bp::AwardAchievementPacket pkt;
    pkt.achievement_id = 0x1234;

    // generated by gophertunnel:
    // packet.AwardAchievement{AchievementID: 0x1234}
    const std::vector<std::uint8_t> golden{
        0x34, 0x12, 0x00, 0x00,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::AwardAchievementPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->achievement_id == 0x1234);
}
