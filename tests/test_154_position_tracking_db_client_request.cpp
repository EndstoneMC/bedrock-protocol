#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("PositionTrackingDBClientRequestPacket: id + round-trip with a QUERY action")
{
    STATIC_REQUIRE(bp::PositionTrackingDBClientRequestPacket::Id == 154);

    bp::PositionTrackingDBClientRequestPacket pkt;
    pkt.action = bp::PositionTrackingDBClientRequestPacket::Action::Query;
    pkt.id = static_cast<bp::PositionTrackingId>(42);

    // generated by gophertunnel:
    // packet.PositionTrackingDBClientRequest{RequestAction: 0, TrackingID: 42}
    const std::vector<std::uint8_t> golden{0x00, 0x54};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::PositionTrackingDBClientRequestPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->action == bp::PositionTrackingDBClientRequestPacket::Action::Query);
    REQUIRE(rt->id == 42);
}
