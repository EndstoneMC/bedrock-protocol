#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("SetLastHurtByPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::SetLastHurtByPacket::Id == 96);

    bp::SetLastHurtByPacket pkt;
    pkt.last_hurt_by = 42;

    // generated by gophertunnel:
    // packet.SetLastHurtBy{EntityType: 42}
    const std::vector<std::uint8_t> golden{0x54};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::SetLastHurtByPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->last_hurt_by == 42);
}
