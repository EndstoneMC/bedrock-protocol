#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("RemoveVolumeEntityPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::RemoveVolumeEntityPacket::Id == 167);

    bp::RemoveVolumeEntityPacket pkt;
    pkt.entity_net_id = static_cast<bp::EntityNetId>(7);
    pkt.dimension_type = static_cast<bp::DimensionType>(0);

    // generated by gophertunnel:
    // packet.RemoveVolumeEntity{EntityRuntimeID: 7, Dimension: 0}
    const std::vector<std::uint8_t> golden{0x07, 0x00};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::RemoveVolumeEntityPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->entity_net_id == static_cast<bp::EntityNetId>(7));
}
