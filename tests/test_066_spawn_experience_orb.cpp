#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("SpawnExperienceOrbPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::SpawnExperienceOrbPacket::Id == 66);

    bp::SpawnExperienceOrbPacket pkt;
    pkt.pos = bp::Vec3{1.5f, 64.0f, -2.5f};
    pkt.xp_value = 12;

    // generated by gophertunnel:
    // packet.SpawnExperienceOrb{Position: {1.5, 64, -2.5}, ExperienceAmount: 12}
    const std::vector<std::uint8_t> golden{
        0x00, 0x00, 0xC0, 0x3F, 0x00, 0x00, 0x80, 0x42, 0x00, 0x00, 0x20, 0xC0, 0x18,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::SpawnExperienceOrbPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->xp_value == 12);
}
