#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ClientCacheStatusPacket: round-trip")
{
    STATIC_REQUIRE(bp::ClientCacheStatusPacket::Id == 129);

    bp::ClientCacheStatusPacket pkt;
    pkt.enabled = true;

    // generated by gophertunnel:
    // packet.ClientCacheStatus{Enabled: true}
    const std::vector<std::uint8_t> golden{
        0x01,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ClientCacheStatusPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->enabled == true);
}
