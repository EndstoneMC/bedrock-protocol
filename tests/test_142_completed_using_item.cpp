#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("CompletedUsingItemPacket: id + round-trip")
{
    STATIC_REQUIRE(bp::CompletedUsingItemPacket::Id == 142);

    bp::CompletedUsingItemPacket pkt;
    pkt.item_id = 42;
    pkt.item_use_method = bp::ItemUseMethod::Attack;

    // generated by gophertunnel:
    // packet.CompletedUsingItem{UsedItemID: 42, UseMethod: UseItemAttack}
    const std::vector<std::uint8_t> golden{0x2A, 0x00, 0x02, 0x00, 0x00, 0x00};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::CompletedUsingItemPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->item_id == 42);
    REQUIRE(rt->item_use_method == bp::ItemUseMethod::Attack);
}
