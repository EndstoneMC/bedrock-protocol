#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("SetCommandsEnabledPacket: id + round-trip with a single bool")
{
    STATIC_REQUIRE(bp::SetCommandsEnabledPacket::Id == 59);

    bp::SetCommandsEnabledPacket pkt;
    pkt.commands_enabled = true;

    // generated by gophertunnel:
    // packet.SetCommandsEnabled{Enabled: true}
    const std::vector<std::uint8_t> golden{0x01};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::SetCommandsEnabledPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->commands_enabled);
}
