#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("RemoveObjectivePacket: id + round-trip with a single objective name")
{
    STATIC_REQUIRE(bp::RemoveObjectivePacket::Id == 106);

    bp::RemoveObjectivePacket pkt;
    pkt.objective_name = "score";

    // generated by gophertunnel:
    // packet.RemoveObjective{ObjectiveName: "score"}
    const std::vector<std::uint8_t> golden{
        0x05, 0x73, 0x63, 0x6F, 0x72, 0x65,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::RemoveObjectivePacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->objective_name == "score");
}
