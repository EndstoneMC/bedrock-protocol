#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("UpdateAttributesPacket: id + round-trip with nested attribute list")
{
    STATIC_REQUIRE(bp::UpdateAttributesPacket::Id == 29);

    bp::AttributeData health;
    health.min_value = 0.0f;
    health.max_value = 20.0f;
    health.current_value = 15.0f;
    health.default_min_value = 0.0f;
    health.default_max_value = 20.0f;
    health.default_value = 20.0f;
    health.name = "minecraft:health";
    health.modifiers = {bp::AttributeModifier{"id", "nm", 1.0f, bp::AttributeModifierOperation::OperationAddition,
                                              bp::AttributeOperands::OperandMin, true}};

    bp::UpdateAttributesPacket pkt;
    pkt.runtime_id = static_cast<bp::ActorRuntimeID>(42);
    pkt.attribute_data = {health};
    pkt.tick = 3;

    // generated by gophertunnel:
    // packet.UpdateAttributes{EntityRuntimeID: 42, Attributes: []protocol.Attribute{{AttributeValue:
    // protocol.AttributeValue{Name: "minecraft:health", Min: 0, Max: 20, Value: 15}, DefaultMin: 0, DefaultMax: 20,
    // Default: 20, Modifiers: []protocol.AttributeModifier{{ID: "id", Name: "nm", Amount: 1, Operation: 0, Operand: 0,
    // Serializable: true}}}}, Tick: 3}
    const std::vector<std::uint8_t> golden{
        0x2A, 0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0xA0, 0x41, 0x00, 0x00, 0x70, 0x41, 0x00, 0x00,
        0x00, 0x00, 0x00, 0x00, 0xA0, 0x41, 0x00, 0x00, 0xA0, 0x41, 0x10, 0x6D, 0x69, 0x6E, 0x65, 0x63,
        0x72, 0x61, 0x66, 0x74, 0x3A, 0x68, 0x65, 0x61, 0x6C, 0x74, 0x68, 0x01, 0x02, 0x69, 0x64, 0x02,
        0x6E, 0x6D, 0x00, 0x00, 0x80, 0x3F, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x01, 0x03,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::UpdateAttributesPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->attribute_data.size() == 1);
    REQUIRE(rt->attribute_data[0].name == "minecraft:health");
    REQUIRE(rt->attribute_data[0].modifiers[0].serialize);
    REQUIRE(rt->tick == 3);
}
