#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ClientCameraAimAssistPacket: id + round-trip with the SET action")
{
    STATIC_REQUIRE(bp::ClientCameraAimAssistPacket::Id == 321);

    bp::ClientCameraAimAssistPacket pkt;
    pkt.camera_preset_id = "x";
    pkt.action = bp::ClientCameraAimAssistPacketAction::SetFromCameraPreset;
    pkt.allow_aim_assist = true;

    // generated by gophertunnel:
    // packet.ClientCameraAimAssist{PresetID: "x", Action: 0, AllowAimAssist: true}
    const std::vector<std::uint8_t> golden{0x01, 0x78, 0x00, 0x01};

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ClientCameraAimAssistPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->camera_preset_id == "x");
    REQUIRE(rt->allow_aim_assist);
}
