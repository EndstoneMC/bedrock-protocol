#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("CurrentStructureFeaturePacket: id + round-trip with a single feature name")
{
    STATIC_REQUIRE(bp::CurrentStructureFeaturePacket::Id == 314);

    bp::CurrentStructureFeaturePacket pkt;
    pkt.current_structure_feature = "village";

    // generated by gophertunnel:
    // packet.CurrentStructureFeature{CurrentFeature: "village"}
    const std::vector<std::uint8_t> golden{
        0x07, 0x76, 0x69, 0x6C, 0x6C, 0x61, 0x67, 0x65,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::CurrentStructureFeaturePacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->current_structure_feature == "village");
}
