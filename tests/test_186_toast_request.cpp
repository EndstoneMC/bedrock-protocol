#include <cstdint>
#include <vector>

#include <bedrock/protocol.hpp>
#include <catch2/catch_test_macros.hpp>

namespace bp = bedrock::protocol;

TEST_CASE("ToastRequestPacket: id + round-trip with title + content")
{
    STATIC_REQUIRE(bp::ToastRequestPacket::Id == 186);

    bp::ToastRequestPacket pkt;
    pkt.title = "Hello";
    pkt.content = "World";

    // generated by gophertunnel:
    // packet.ToastRequest{Title: "Hello", Message: "World"}
    const std::vector<std::uint8_t> golden{
        0x05, 0x48, 0x65, 0x6C, 0x6C, 0x6F, 0x05, 0x57, 0x6F, 0x72, 0x6C, 0x64,
    };

    std::vector<std::uint8_t> buf;
    bp::BinaryStream out{buf};
    bp::serialize(out, pkt);
    REQUIRE(buf == golden);

    bp::BinaryReader in{buf};
    auto rt = bp::deserialize<bp::ToastRequestPacket>(in);
    REQUIRE(rt.has_value());
    REQUIRE(in.getUnreadLength() == 0);
    REQUIRE(rt->title == "Hello");
    REQUIRE(rt->content == "World");
}
